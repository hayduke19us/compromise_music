<div class="playlist_pg" id="current_page"></div>
<div class="container">
  <div class="row">
    <div class="col-md-3 pull-left">
      <% playlists.each do |playlist| %>
        <%= render partial: "/playlists/playlist_thumbnail", 
          locals: {playlist: playlist} %> 
      <%end%>
    </div>
  </div>
</div>
