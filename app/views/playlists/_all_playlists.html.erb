<div class="container">
  <div class="row">
    <div class="col-md-10">
      <% playlists.each do |playlist| %>
        <div id="all_pl_thumb">
          <%= render partial: "/playlists/playlist_thumbnail", 
            locals: {playlist: playlist} %> 
        </div>
      <%end%>
    </div>
  </div>
</div>
